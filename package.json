{
  "name": "ai-creator-board-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "Codex marketplace and MCP App for AI creator workflows",
  "license": "MIT",
  "scripts": {
    "build": "npm run build --prefix plugins/ai-creator-board",
    "typecheck": "npm run typecheck --prefix plugins/ai-creator-board",
    "test": "npm run test --prefix plugins/ai-creator-board",
    "test:ui": "npm run test:ui --prefix plugins/ai-creator-board",
    "validate": "npm run validate --prefix plugins/ai-creator-board",
    "check": "npm run typecheck && npm run test && npm run build && npm run validate"
  },
  "engines": {
    "node": ">=20"
  }
}
